# data used in the vignette
data <- read.csv("data-raw/Stress_Dataset.csv")

use_data(data)

# data used in the readme
readme_data <- read.csv("data-raw/likert_sample_data.csv")

use_data(readme_data)